# ProofPath payment profile v0.1

This profile defines a simulation-only action context for `agent_payment` decisions.

Decision outputs are `ACCEPT`, `BLOCK`, or `HOLD` and must be appended to `.proofpath/audit.jsonl`.

The v0.1 profile is deterministic and local by design.
